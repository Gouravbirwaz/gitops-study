apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd

data:

  # Global context values usable in templates
  context: |
    argocdUrl: "http://localhost:5000"

  # Global subscriptions (applies to all applications)
  subscriptions: |
    - recipient: email:gouravbirwaz@gmail.com
      triggers:
        - on-health-degraded
        - on-deployed

  # Email service configuration (Gmail SMTP)
  service.email: |
    username: $email-username
    password: $email-password
    host: smtp.gmail.com
    port: 587
    from: $email-username

  # Template: Application Health Degraded
  template.email-health-degraded: |
    email:
      subject: "[ArgoCD] {{.app.metadata.name}} health is {{.app.status.health.status}}"
      body: |
        🚨 Application: {{.app.metadata.name}}
        📂 Namespace: {{.app.metadata.namespace}}
        🔄 Sync Status: {{.app.status.sync.status}}
        📌 Revision: {{.app.status.sync.revision}}
        ❤️ Health Status: {{.app.status.health.status}}
        📝 Health Message: {{.app.status.health.message}}
        🔗 Details: {{.context.argocdUrl}}/applications/{{.app.metadata.name}}

  # Template: Application Successfully Deployed
  template.email-deployed: |
    email:
      subject: "[ArgoCD] {{.app.metadata.name}} successfully deployed 🎉"
      body: |
        ✅ Application: {{.app.metadata.name}}
        📂 Namespace: {{.app.metadata.namespace}}
        🔄 Sync Status: {{.app.status.sync.status}}
        📌 Revision: {{.app.status.sync.revision}}
        ❤️ Health Status: {{.app.status.health.status}}
        ⏱ Finished At: {{ if .app.status.operationState }}{{ .app.status.operationState.finishedAt }}{{ end }}
        🔗 Details: {{.context.argocdUrl}}/applications/{{.app.metadata.name}}

  # Trigger: Send email when health becomes Degraded (only once per degradation)
  trigger.on-health-degraded: |
    - when: app.status.health.status == 'Degraded'
      oncePer: app.status.health.status
      send:
        - email-health-degraded

  # Trigger: Send email when sync succeeds AND health is Healthy
  trigger.on-deployed: |
    - when: app.status.operationState != nil and app.status.operationState.phase == 'Succeeded' and app.status.health.status == 'Healthy'
      oncePer: app.status.operationState.finishedAt
      send:
        - email-deployed